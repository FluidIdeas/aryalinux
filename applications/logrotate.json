{
  "name": "logrotate",
  "version": "3.22.0",
  "description": "The logrotate package allows automatic rotation, compression, removal, and mailing of log files.",
  "url": "https://github.com/logrotate/logrotate/releases/download/3.22.0/logrotate-3.22.0.tar.xz",
  "dependencies": {
    "required": [
      "popt"
    ]
  },
  "build": {
    "user": [
      "./configure --prefix=/usr",
      "make"
    ],
    "package": [
      "make install DESTDIR=$DESTDIR",
      "mkdir -p $DESTDIR/etc/logrotate.d",
      "cat > $DESTDIR/etc/logrotate.d/sys.log << EOF\n/var/log/sys.log {\n   # If the log file is larger than 100kb, rotate it\n   size   100k\n   rotate 5\n   weekly\n   postrotate\n      /bin/killall -HUP syslogd\n   endscript\n}\nEOF",
      "chmod -v 0644 $DESTDIR/etc/logrotate.d/sys.log",
      "cat > $DESTDIR/etc/logrotate.d/example.log << EOF\nfile1\nfile2\nfile3 {\n   ...\n   postrotate\n    ...\n   endscript\n}\nEOF",
      "chmod -v 0644 $DESTDIR/etc/logrotate.d/example.log",
      "cat > $DESTDIR/etc/logrotate.conf << EOF\n# Begin /etc/logrotate.conf\n\n# Rotate log files weekly\nweekly\n\n# Don't mail logs to anybody\nnomail\n\n# If the log file is empty, it will not be rotated\nnotifempty\n\n# Number of backups that will be kept\n# This will keep the 2 newest backups only\nrotate 2\n\n# Create new empty files after rotating old ones\n# This will create empty log files, with owner\n# set to root, group set to sys, and permissions 664\ncreate 0664 root sys\n\n# Compress the backups with gzip\ncompress\n\n# No packages own lastlog or wtmp -- rotate them here\n/var/log/wtmp {\n    monthly\n    create 0664 root utmp\n    rotate 1\n}\n\n/var/log/lastlog {\n    monthly\n    rotate 1\n}\n\n# Some packages drop log rotation info in this directory\n# so we include any file in it.\ninclude /etc/logrotate.d\n\n# End /etc/logrotate.conf\nEOF",
      "chmod -v 0644 $DESTDIR/etc/logrotate.conf",
      "cat > $DESTDIR/usr/lib/systemd/system/logrotate.service << \"EOF\"\n[Unit]\nDescription=Runs the logrotate command\nDocumentation=man:logrotate(8)\nDefaultDependencies=no\nAfter=local-fs.target\nBefore=shutdown.target\n\n[Service]\nType=oneshot\nRemainAfterExit=yes\nExecStart=/usr/sbin/logrotate /etc/logrotate.conf\nEOF",
      "cat > $DESTDIR/usr/lib/systemd/system/logrotate.timer << \"EOF\"\n[Unit]\nDescription=Runs the logrotate command daily at 3:00 AM\n\n[Timer]\nOnCalendar=*-*-* 3:00:00\nPersistent=true\n\n[Install]\nWantedBy=timers.target\nEOF"
    ]
  },
  "post_install": [
    "systemctl enable logrotate.timer"
  ],
  "meta": false,
  "book": "general/logrotate.html",
  "category": "libraries",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
