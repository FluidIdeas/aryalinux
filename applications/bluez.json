{
  "name": "bluez",
  "version": "5.86",
  "description": "The BlueZ package contains the Bluetooth protocol stack for Linux.",
  "url": [
    "https://www.kernel.org/pub/linux/bluetooth/bluez-5.86.tar.xz"
  ],
  "dependencies": {
    "required": [
      "dbus",
      "glib2",
      "libical",
      "python-modules.docutils"
    ]
  },
  "build": {
    "user": [
      "sed -i '4967,4968d' src/adapter.c",
      "./configure --prefix=/usr         \\\n            --sysconfdir=/etc     \\\n            --localstatedir=/var  \\\n            --enable-library",
      "make ICAL_LIBS=\"-lical -licalvcal\""
    ],
    "package": [
      "make install DESTDIR=$DESTDIR",
      "install -d $DESTDIR/usr/sbin",
      "ln -sfv ../libexec/bluetooth/bluetoothd $DESTDIR/usr/sbin/bluetoothd",
      "install -d $DESTDIR/etc/bluetooth",
      "install -m644 src/main.conf $DESTDIR/etc/bluetooth/main.conf",
      "install -vdm755 $DESTDIR/etc/bluetooth",
      "cat > $DESTDIR/etc/bluetooth/rfcomm.conf << \"EOF\"\n# Start rfcomm.conf\n# Set up the RFCOMM configuration of the Bluetooth subsystem in the Linux kernel.\n# Use one line per command\n# See the rfcomm man page for options\n\n\n# End of rfcomm.conf\nEOF",
      "cat > $DESTDIR/etc/bluetooth/uart.conf << \"EOF\"\n# Start uart.conf\n# Attach serial devices via UART HCI to BlueZ stack\n# Use one line per device\n# See the hciattach man page for options\n\n# End of uart.conf\nEOF"
    ]
  },
  "post_install": [
    "systemctl enable bluetooth",
    "systemctl enable --global obex"
  ],
  "meta": false,
  "book": "general/bluez.html",
  "category": "libraries",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
