{
  "name": "bluez",
  "version": "5.86",
  "description": "The BlueZ package contains the Bluetooth protocol stack for Linux.",
  "url": "https://www.kernel.org/pub/linux/bluetooth/bluez-5.86.tar.xz",
  "dependencies": {
    "required": [
      "dbus",
      "glib2",
      "python-modules.docutils"
    ]
  },
  "build": {
    "user": [
      "sed -i '4967,4968d' src/adapter.c",
      "./configure --prefix=/usr         \\\n            --sysconfdir=/etc     \\\n            --localstatedir=/var  \\\n            --enable-library",
      "make",
      "ln -svf ../libexec/bluetooth/bluetoothd /usr/sbin",
      "cat > /etc/bluetooth/rfcomm.conf << \"EOF\"\n# Start rfcomm.conf\n# Set up the RFCOMM configuration of the Bluetooth subsystem in the Linux kernel.\n# Use one line per command\n# See the rfcomm man page for options\n\n\n# End of rfcomm.conf\nEOF\n          cat > /etc/bluetooth/uart.conf << \"EOF\"\n# Start uart.conf\n# Attach serial devices via UART HCI to BlueZ stack\n# Use one line per device\n# See the hciattach man page for options\n\n# End of uart.conf\nEOF\n        \n        \n          \n             Systemd Bluez Services\n          \n          \n            To start the bluetoothd daemon at boot,\n            enable the previously installed systemd unit by running the\n            following command as the root\n            user:\n          \n          systemctl enable bluetooth",
      "systemctl enable --global obex"
    ],
    "package": [
      "make install DESTDIR=$DESTDIR",
      "install -v -dm555 $DESTDIR/etc/bluetooth",
      "install -v -m644 src/main.conf $DESTDIR/etc/bluetooth/main.conf",
      "install -v -dm755 $DESTDIR/usr/share/doc/bluez-5.86",
      "install -v -m644 doc/*.txt $DESTDIR/usr/share/doc/bluez-5.86"
    ]
  },
  "meta": false,
  "book": "general/bluez.html",
  "category": "libraries",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
