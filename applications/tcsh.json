{
  "name": "tcsh",
  "version": "6.24.16",
  "description": "The Tcsh package contains “an enhanced but completely compatible version of the Berkeley Unix C shell (csh).” This is useful as an alternative shell for those who prefer C syntax to that of the bash shell, and also because some programs require the C shell in order to perform installation tasks.",
  "url": [
    "https://astron.com/pub/tcsh/tcsh-6.24.16.tar.gz"
  ],
  "dependencies": {},
  "build": {
    "user": [
      "./configure --prefix=/usr",
      "make"
    ],
    "package": [
      "make install install.man DESTDIR=$DESTDIR",
      "ln -v -sf tcsh   $DESTDIR/bin/csh",
      "ln -v -sf tcsh.1 $DESTDIR/usr/share/man/man1/csh.1",
      "cat >> $DESTDIR/etc/shells << \"EOF\"\n/bin/tcsh\n/bin/csh\nEOF"
    ]
  },
  "post_install": [],
  "meta": false,
  "book": "postlfs/tcsh.html",
  "category": "system",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
