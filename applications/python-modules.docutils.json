{
  "name": "python-modules.docutils",
  "version": "0.22.4",
  "description": "docutils is a set of Python modules and programs for processing plaintext docs into formats such as HTML, XML, or LaTeX.",
  "url": "https://files.pythonhosted.org/packages/source/d/docutils/docutils-0.22.4.tar.gz",
  "dependencies": {},
  "build": {
    "user": [
      "pip3 wheel -w dist --no-build-isolation --no-deps --no-cache-dir $PWD"
    ],
    "package": [
      "for f in $DESTDIR/usr/bin/rst*.py; do\n  rm -fv $DESTDIR/usr/bin/$(basename $f .py)\ndone",
      "pip3 install --root=$DESTDIR --no-index --find-links dist --no-user docutils"
    ]
  },
  "meta": false,
  "book": "general/python-modules.html",
  "category": "python-module",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
