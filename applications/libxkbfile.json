{
  "name": "libxkbfile",
  "version": "1.2.0",
  "description": "Xorg library libxkbfile (BLFS chapter).",
  "url": [
    "https://xorg.freedesktop.org/archive/individual/lib/libxkbfile-1.2.0.tar.xz"
  ],
  "dependencies": {
    "required": [
      "libpciaccess",
      "libxcb"
    ]
  },
  "build": {
    "environment": {
      "XORG_PREFIX": "/usr",
      "XORG_CONFIG": "--prefix=/usr --sysconfdir=/etc --localstatedir=/var --disable-static"
    },
    "user": [
      "rm -rf build",
      "meson setup build --prefix=$XORG_PREFIX --buildtype=release",
      "ninja -C build"
    ],
    "package": [
      "DESTDIR=$DESTDIR ninja -C build install"
    ]
  },
  "post_install": [],
  "meta": false,
  "book": "x/libxkbfile.html",
  "category": "x11",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
