{
  "name": "fcron",
  "version": "3.4.0",
  "description": "The Fcron package contains a periodical command scheduler which aims at replacing Vixie Cron.",
  "url": "http://fcron.free.fr/archives/fcron-3.4.0.src.tar.gz",
  "dependencies": {},
  "build": {
    "user": [
      "find doc -type f -exec sed -i 's:/usr/local::g' {} \\;",
      "./configure --prefix=/usr        \\\n            --sysconfdir=/etc    \\\n            --localstatedir=/var \\\n            --without-sendmail   \\\n            --with-piddir=/run   \\\n            --with-boot-install=no",
      "make"
    ],
    "package": [
      "make install DESTDIR=$DESTDIR",
      "install -dm754 $DESTDIR/etc/cron.{hourly,daily,weekly,monthly}",
      "cat > $DESTDIR/usr/bin/run-parts << \"EOF\"\n#!/bin/sh\n# run-parts:  Runs all the scripts found in a directory.\n\nset +e\n\nif [ $# -lt 1 ]; then\n  echo \"Usage: run-parts <directory>\"\n  exit 1\nfi\n\nif [ ! -d $1 ]; then\n  echo \"Not a directory: $1\"\n  echo \"Usage: run-parts <directory>\"\n  exit 1\nfi\n\nIGNORE_SUFFIXES=\"~ ^ , .bak .new .rpmsave .rpmorig .rpmnew .swp\"\n\nfor SCRIPT in $1/* ; do\n  if [ ! -f $SCRIPT ]; then\n    continue\n  fi\n  SKIP=false\n  for SUFFIX in $IGNORE_SUFFIXES ; do\n    if [ ! \"$(basename $SCRIPT $SUFFIX)\" = \"$(basename $SCRIPT)\" ]; then\n      SKIP=true\n      break\n    fi\n  done\n  if [ \"$SKIP\" = \"true\" ]; then\n    continue\n  fi\n  if [ -x $SCRIPT ]; then\n    $SCRIPT || echo \"$SCRIPT failed.\"\n  fi\ndone\n\nexit 0\nEOF",
      "chmod 755 $DESTDIR/usr/bin/run-parts",
      "cat > $DESTDIR/var/spool/fcron/systab.orig << \"EOF\"\n&bootrun 01 * * * * root run-parts /etc/cron.hourly\n&bootrun 02 4 * * * root run-parts /etc/cron.daily\n&bootrun 22 4 * * 0 root run-parts /etc/cron.weekly\n&bootrun 42 4 1 * * root run-parts /etc/cron.monthly\nEOF"
    ]
  },
  "post_install": [
    "getent group fcron >/dev/null || groupadd -g 22 fcron",
    "id -u fcron >/dev/null 2>&1 || useradd -d /dev/null -c \"Fcron User\" -g fcron -s /bin/false -u 22 fcron",
    "systemctl enable fcron",
    "fcrontab -z -u systab"
  ],
  "meta": false,
  "book": "general/fcron.html",
  "category": "libraries",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
