{
  "name": "python-modules.cachecontrol",
  "version": "0.14.4",
  "description": "CacheControl is a port of the caching algorithms in httplib2 for use with requests session object. It was written because httplib2's better support for caching is often mitigated by its lack of thread safety. The same is true of requests in terms of caching.",
  "url": [
    "https://github.com/psf/cachecontrol/archive/v0.14.4/cachecontrol-0.14.4.tar.gz"
  ],
  "dependencies": {
    "required": [
      "python-modules.requests",
      "python-deps.msgpack",
      "python-deps.uv_build"
    ]
  },
  "build": {
    "user": [
      "pip3 wheel -w dist --no-build-isolation --no-deps --no-cache-dir $PWD"
    ],
    "package": [
      "pip3 install --root=$DESTDIR --no-index --find-links dist --no-user cachecontrol"
    ]
  },
  "post_install": [],
  "meta": false,
  "book": "general/python-modules.html",
  "category": "python-module",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
