{
  "name": "xmodmap",
  "version": "1.0.11",
  "description": "Xorg application xmodmap (BLFS chapter).",
  "url": [
    "https://xorg.freedesktop.org/archive/individual/app/xmodmap-1.0.11.tar.xz"
  ],
  "dependencies": {
    "required": [
      "xmessage"
    ]
  },
  "build": {
    "environment": {
      "XORG_PREFIX": "/usr",
      "XORG_CONFIG": "--prefix=/usr --sysconfdir=/etc --localstatedir=/var --disable-static"
    },
    "user": [
      "./configure $XORG_CONFIG",
      "make"
    ],
    "package": [
      "make install DESTDIR=$DESTDIR"
    ]
  },
  "post_install": [],
  "meta": false,
  "book": "x/xmodmap.html",
  "category": "x11",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
