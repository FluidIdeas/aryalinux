{
  "name": "iceauth",
  "version": "1.0.10",
  "description": "Xorg application iceauth (BLFS chapter).",
  "url": [
    "https://xorg.freedesktop.org/archive/individual/app/iceauth-1.0.10.tar.xz"
  ],
  "dependencies": {
    "required": [
      "libpng",
      "mesa",
      "xcb-util",
      "x7lib"
    ]
  },
  "build": {
    "environment": {
      "XORG_PREFIX": "/usr",
      "XORG_CONFIG": "--prefix=/usr --sysconfdir=/etc --localstatedir=/var --disable-static"
    },
    "user": [
      "./configure $XORG_CONFIG",
      "make"
    ],
    "package": [
      "make install DESTDIR=$DESTDIR"
    ]
  },
  "post_install": [],
  "meta": false,
  "book": "x/iceauth.html",
  "category": "x11",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  }
}
