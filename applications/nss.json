{
  "name": "nss",
  "version": "3.120.1",
  "description": "The Network Security Services (NSS) package is a set of libraries designed to support cross-platform development of security-enabled client and server applications. Applications built with NSS can support SSL v2 and v3, TLS, PKCS #5, PKCS #7, PKCS #11, PKCS #12, S/MIME, X.509 v3 certificates, and other security standards. This is useful for implementing SSL and S/MIME or other Internet security standards into an application.",
  "url": [
    "https://archive.mozilla.org/pub/security/nss/releases/NSS_3_120_1_RTM/src/nss-3.120.1.tar.gz"
  ],
  "dependencies": {
    "required": [
      "nspr"
    ]
  },
  "build": {
    "user": [
      "patch -Np1 -i ../nss-standalone-1.patch",
      "(cd nss && make BUILD_OPT=1                      \\\n  NSPR_INCLUDE_DIR=/usr/include/nspr  \\\n  USE_SYSTEM_ZLIB=1                   \\\n  ZLIB_LIBS=-lz                       \\\n  NSS_ENABLE_WERROR=0                 \\\n  NSS_USE_SYSTEM_SQLITE=1             \\\n  $([ $(uname -m) = x86_64 ] && echo USE_64=1))"
    ],
    "package": [
      "install -v -dm755 $DESTDIR/usr/lib $DESTDIR/usr/lib/pkgconfig $DESTDIR/usr/bin $DESTDIR/usr/include/nss",
      "(cd dist && install -v -m755 Linux*/lib/*.so              $DESTDIR/usr/lib && \\\n install -v -m644 Linux*/lib/{*.chk,libcrmf.a} $DESTDIR/usr/lib && \\\n cp -v -RL {public,private}/nss/*              $DESTDIR/usr/include/nss && \\\n install -v -m755 Linux*/bin/{certutil,nss-config,pk12util} $DESTDIR/usr/bin && \\\n install -v -m644 Linux*/lib/pkgconfig/nss.pc  $DESTDIR/usr/lib/pkgconfig && \\\n ln -sfv ./pkcs11/p11-kit-trust.so $DESTDIR/usr/lib/libnssckbi.so)"
    ]
  },
  "post_install": [],
  "meta": false,
  "book": "postlfs/nss.html",
  "category": "security",
  "install": {
    "extract": [
      "tar -xJf $PACKAGE -C /"
    ]
  },
  "additionalUrls": [
    "https://www.linuxfromscratch.org/patches/blfs/13.0/nss-standalone-1.patch"
  ]
}
